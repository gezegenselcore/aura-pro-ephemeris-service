{
  "name": "aura-pro-ephemeris-service-root",
  "version": "1.0.0",
  "private": true,
  "description": "Root for scripts (smoke test) — no Firebase client SDK",
  "scripts": {
    "smoke:astrochart": "node scripts/test-deployed-astrochart.js",
    "smoke": "node scripts/test-deployed-astrochart.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0"
  }
}
